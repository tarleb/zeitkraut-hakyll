<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN"
 "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg viewBox="0 0 212 212"
     xmlns="http://www.w3.org/2000/svg"
     xmlns:xlink="http://www.w3.org/1999/xlink" >
  <g transform="translate(100 100)">
    <!-- hours -->
    <g class="hours" transform="rotate(310)">
      <rect class="dial" x="-11" y="-50" width="22" height="50" ry="16px" fill="#316189"/>
    </g>
    <!-- minutes -->
    <g class="minutes" transform="rotate(60)">
      <rect class="dial" x="-10" y="-70" width="20" height="70" ry="14px" fill="#316189"/>
    </g>
    <!-- seconds -->
    <g class="seconds" transform="rotate(200)">
      <circle class="centerpiece" cx="0" cy="0" r="2" stroke="none"  fill="#316189" />
      <rect class="dial" x="-2" y="-73" width="4" height="73" ry="5px" fill="#316189"/>
    </g>
    <!-- clock center -->
    <circle class="centerpiece hours minutes" cx="0" cy="0" r="15" stroke="none" fill="#316189"/>
  </g>
  <g class="lupe" transform="translate(100 100) rotate(45)">
    <!-- lens -->
    <circle class="lens" cx="0" cy="0" r="86" fill="none" stroke="#316189" stroke-width="26px" />
    <!-- handle -->
    <rect class="handle" x="90" y="-30px" width="40" height="60px" rx="5px" fill="#316189" />
  </g>
  <script type="application/javascript"><![CDATA[
(function () {
var clock = function() {
  var sDial = document.getElementsByClassName("seconds")[0];
  var mDial = document.getElementsByClassName("minutes")[0];
  var hDial = document.getElementsByClassName("hours")[0];

  function moveDials(hours, minutes, seconds) {
    hDeg = (360 / 12) * (hours + minutes/60);
    mDeg = (360 / 60) * minutes;
    sDeg = (360 / 60) * seconds;
    sDial.setAttribute("transform", "rotate("+sDeg+")");
    mDial.setAttribute("transform", "rotate("+mDeg+")");
    hDial.setAttribute("transform", "rotate("+hDeg+")");
  }

  function update() {
    var currentTime = new Date();
    moveDials(currentTime.getHours(), currentTime.getMinutes(), currentTime.getSeconds());
  };
  update();
};
setInterval(clock, 50);
})();
  ]]></script>
</svg>
